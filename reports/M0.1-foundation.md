# M0.1 foundation engineering record

## Discovered state and scope

The local workspace contained only the owner's root `AGENTS.md`, with no Git metadata. Authenticated GitHub discovery identified the existing empty `dylanlaewe/EdgeLab` repository; `git ls-remote` returned no refs. Initialized local `main` and set its origin to that repository. Preserved root instructions unchanged.

Implemented governance, seven role contracts, durable handoffs, nine strict JSON schemas, scientific/temporal/lineage specifications, explicit lifecycle and STOP/kill-switch design, conceptual bankroll and fail-closed M0 policy. No external provider dependency, research results, approvals or trading implementation were created.

## Decisions and limits

Plain Markdown and versioned JSON keep the institution auditable without speculative databases or services. Python plus pinned JSON Schema validation supplies a small testable foundation. Integer cents avoid money rounding ambiguity. Zero live limits implement M0 prohibition; statistical and future risk thresholds remain unresolved. Git provides reviewable history, not authenticated or tamper-proof event storage.

Schema validation is necessary but insufficient. Cross-record scientific evidence, independent reviewer authentication, lifecycle execution, STOP dependency enforcement and operational kill switches remain explicitly unimplemented. M0.1 is an engineering increment; the overall M0 milestone is incomplete. Director cannot declare this design independently approved.

## Validation

Executed on Python 3.13.4 in an isolated virtual environment with pinned dependencies:

- `.venv/bin/python -m unittest discover -s tests -v`: 18 tests passed.
- `.venv/bin/python -m src.edgelab.validate`: passed; all persisted typed records validated and capital LOCKED.

Tests cover schema checks, capital mutations, every zero exposure limit, gate removal/replacement/duplication, invalid timestamps, missing portfolio state, duplicate JSON keys and identities, missing handoff evidence, unsupported live states, STOP resolution consistency, access evidence, protocol ordering, missing preregistration and unresolved thresholds. Fixtures are synthetic, not sports-market evidence. No backtest, external ingestion, paper execution, or live execution was performed.

Validation does not attest to the scientific truth of records, actor identity or permissions. Final Git commit and push results are reported by the Director after those commands execute; this record makes no advance claim of successful publication.

## Next assignment

Bring **05 Skeptic** online using `docs/handoffs/H-M0-001.v1.json`. Review this foundation independently before treating it as the approved design for Data/Risk implementation. No specialized session was started by this milestone.
